
describe('formatPrice function', () => {
  it('formats number with default currency and 2 decimals', () => { });
  it('formats number with custom currency symbol and decimals', () => {});
  it('returns empty string for invalid input', () => {});
})
describe('HeroesFromAPI component', () => {
  it('renders "No heroes available" when API returns empty list',()=>{});
  it('renders heroes fetched from API', () => {});
});
