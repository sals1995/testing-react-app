describe("Status component", () => {
  it('should render "🔴 Offline" and the toggle button on initial render', () => {});
  
  it('should switch to "🟢 Online" when button is clicked once', () => {});
  
  it('should switch back to "🔴 Offline" when button is clicked twice', () => {});
});

describe("HeroesFromAPI component", () => {
  it('should display "No heroes available" when API returns an empty list', () => {});

  it("should render a list of heroes after successful API fetch", () => {});

  it.skip("BONUS: should display an error message when API request fails with status 500", () => {});
});